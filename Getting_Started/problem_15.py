# what is the output of this code?
x = 2
if x == 2:
    print (x)
else:
    x +

#result is : Syntax error : even if the 'else' is not executed, python looks for syntax errors, so if the else block has a syntax error in it, the program will not execute until you resolve the syntax error.
